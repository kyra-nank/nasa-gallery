module.exports = {
  NASA_KEY: process.env.NASA_KEY
}


// METHOD CAUSED ERRORS (I couldn't resolve them, so I commented it out)

// if (process.env.NODE_ENV === 'production') {
//   module.exports = require('./prod');
// } else {
//   module.exports = require('./dev');
// }